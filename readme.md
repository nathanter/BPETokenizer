# BPE TOKENIZER + Trained Models

This is a simple BPE tokenizer from scratch in python. It does not handle special tokens by reading them from input texts. This is from the off chance this tokenizer must tokenize a tech article talking about special tokens. Instead special tokens are added during the encoding process, but this comes with the tradeoff of much less robustness.

